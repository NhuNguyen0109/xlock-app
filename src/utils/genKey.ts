export const genPublicKey = () => {
  const publicKey = "public";
  console.log(publicKey);
  return publicKey;
};

export const genPrivateKey = () => {
  const privateKey = "private";
  console.log(privateKey);
  return privateKey;
};
